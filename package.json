{
    "name": "ts-classnames",
    "version": "0.1.0",
    "description": "",
    "main": "dist/index.js",
    "scripts": {
        "jest": "jest",
        "tsc": "tsc",
        "build": "rm -rf dist && tsc --project tsconfig.build.json && cp entry/* dist/",
        "watch": "tsc --project tsconfig.build.json --watch",
        "test": "npm run tsc && npm run jest",
        "prepublishOnly": "npm test && npm run build"
    },
    "files": [
        "dist",
        "postcss.js"
    ],
    "author": "",
    "license": "ISC",
    "devDependencies": {
        "@types/jest": "^24.0.21",
        "@types/node": "^12.12.5",
        "jest": "^24.9.0",
        "prettier": "^1.18.2",
        "sh-thunk": "^0.3.0",
        "@types/lodash.debounce": "^4.0.6",
        "ts-jest": "^24.1.0",
        "typescript": "^3.6.4"
    },
    "dependencies": {
        "classnames": "^2.2.6",
        "lodash.debounce": "^4.0.8",
        "postcss": "^7.0.21",
        "postcss-selector-parser": "^6.0.2"
    }
}
